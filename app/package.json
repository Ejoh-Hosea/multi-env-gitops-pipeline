{
  "name": "gitops-demo-app",
  "version": "1.0.0",
  "description": "Demo microservice for multi-environment GitOps pipeline",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/*.test.js",
    "lint": "eslint src/ --ext .js || true"
  },
  "dependencies": {
    "express": "^4.19.2",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
