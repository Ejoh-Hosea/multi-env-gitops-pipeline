apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-app
  labels:
    app: gitops-demo-app
spec:
  selector:
    app: gitops-demo-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
